package Homework1;

import static java.lang.Math.*;

class Example13 {
    public static void main(String[] args) {
        int x = 10;
        double y;
        y = pow(Math.sqrt((pow((-2), 5) + x)), E) * (1 / Math.sqrt((pow(x, 2) + (pow(x, 4) + Math.log(abs(x - 3.14))))));
        System.out.println(y);
    }
}
