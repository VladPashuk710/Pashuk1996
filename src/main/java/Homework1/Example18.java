package Homework1;

import static java.lang.Math.*;

public class Example18 {
    public static void main(String[] args) {
        int x = 10;
        double y;
        y = ((abs(7.2 - 10 * x)) / (pow(Math.sqrt(pow(x, 2) + pow(E, x)), 3) * atan((4 * x / 3) / (Math.sqrt(pow(1.1, 3) + pow(x, 2))))));
        System.out.println(y);
    }
}