package Homework1;

import static java.lang.Math.*;

class Example10 {
    public static void main(String[] args) {
        int x = 10;
        double y;
        y = 1 / 3 * (Math.sqrt(abs(sin(x)))) * (pow(Math.sqrt(pow(E, 0.12 * x)), 3));
        System.out.println(y);
    }
}
